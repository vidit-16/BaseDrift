# Python 3.10+ (developed and measured on 3.12).
# Pure Python: no compiled extensions, no build step.

requests>=2.28.0

# Webhook handler (src/webhook.py). The decision engine, the ablation and
# the rules eval all run without these — install them only to serve or demo
# the payout.pending endpoint.
fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0        # fastapi.testclient, used by tests/test_webhook.py

# Starlette's request.form() requires this for ANY form body, not just
# multipart — the name is misleading. POST /case/{id}/action is an ordinary
# urlencoded HTML form and still needs it. Omitting it passed locally, where
# something else had already pulled it in, and failed on every CI run: twelve
# tests red with "The `python-multipart` library must be installed to use form
# parsing." Pin it here rather than rely on a transitive dependency.
python-multipart>=0.0.9
