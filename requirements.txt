# Python 3.10+ (developed and measured on 3.12).
# Pure Python: no compiled extensions, no build step.

requests>=2.28.0

# Webhook handler (src/webhook.py). The decision engine, the ablation and
# the rules eval all run without these — install them only to serve or demo
# the payout.pending endpoint.
fastapi>=0.111.0
uvicorn>=0.30.0
httpx>=0.27.0        # fastapi.testclient, used by tests/test_webhook.py
